<UserControl x:Class="EzerLaMoreh.CustomUser_Controls.OkCancelButtons"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="100">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Button Content="אישור"  Style="{StaticResource ResourceKey=OkButton}" Command="{Binding Path=OKCommand}"   />
        <Button Content="ביטול" Style="{StaticResource ResourceKey=CancelButton}"   Grid.Row="1" Command="{Binding Path=CloseCommand}"  />
    </Grid>
</UserControl>
